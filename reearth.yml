id: reearth-plugin-mylocation
name: Tetete Plug-in
version: 1.0.0
extensions:
  - id: mylocation
    type: widget
    name: MyLocation Widget
    widgetLayout:
      extendable:
        horizontally: true
        vertically: true